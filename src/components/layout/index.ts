/**
 * Layout Components Barrel Export
 * 
 * This file provides centralized exports for all layout-related components,
 * making it easier to import multiple layout components from a single location.
 * 
 * @example
 * ```tsx
 * import { TopNav, ThemeProvider } from '@/components/layout'
 * ```
 */

export { TopNav } from './TopNav'
export { ThemeProvider } from './ThemeProvider'

export type { TopNavProps } from './TopNav'
export type { ThemeProviderProps } from './ThemeProvider'