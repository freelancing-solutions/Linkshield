# API Configuration (Client-side - accessible in browser)
NEXT_PUBLIC_API_BASE_URL=https://api.linkshield.site/api/v1

# Environment (Client-side)
NEXT_PUBLIC_ENV=development

# Feature Flags (Client-side)
NEXT_PUBLIC_ENABLE_ANALYTICS=false
NEXT_PUBLIC_ENABLE_SOCIAL_PROTECTION=true

# Server-side only variables (NOT accessible in browser)
# Add server-only secrets here without NEXT_PUBLIC_ prefix
# DATABASE_URL=postgresql://...
# JWT_SECRET=your-secret-key
# STRIPE_SECRET_KEY=sk_test_...
